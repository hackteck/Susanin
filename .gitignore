# Logs
logs
*.log
npm-debug.log*

node_modules
dist
dist-ssr
*.local
.vercel

# Env
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Scratch files from local poking around — never part of the project.
*.tmp.*
*_tmp.*

# Generated by `npx cap add android` in the Android workflow; never committed.
/android
/ios

# Cached copy of the real dataset, pulled once by tools/stub-upstream.mjs.
tools/stub-db.json

# The basemap. `npm run basemap` cuts it out of a pinned Protomaps planet build
# and caches the go-pmtiles CLI that does the cutting; tools/build-basemap.mjs is
# what is committed, not its 6 MB of tiles or a 59 MB binary.
/apps/web/public/tiles/
/.cache/
